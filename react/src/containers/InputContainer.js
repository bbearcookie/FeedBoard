import React from 'react';
import { useDispatch } from 'react-redux';

const InputContainer = ({ fieldName }) => {
  const dispatch = useDispatch();

  return (
    <div>
      
    </div>
  );
};

export default React.memo(InputContainer);